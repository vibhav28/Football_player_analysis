[pytest]
# So `import app...` resolves regardless of the directory pytest is
# invoked from — the backend/pipeline code is only ever run with the
# project root on the path (see app/backend/main.py's own docstring).
pythonpath = .
testpaths = tests
